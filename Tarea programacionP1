#include <stdio.h>

int main() {
    int codigo, stock = 0;
    char nombre[50];
    float precio, ganancias = 0;
    int opcion, cantidadVenta, cantidadAgregar;
    float total;

    printf(" Registro del producto \n");
    printf("Ingrese el codigo del producto: ");
    scanf("%d", &codigo);

    printf("Ingrese el nombre del producto: ");
    scanf("%s", nombre);

    printf("Ingrese el stock inicial: ");
    scanf("%d", &stock);

    printf("Ingrese el precio unitario: ");
    scanf("%f", &precio);

    